import { LevelUpQuestion } from './part1';

export const LEVEL_UP_PART_3: LevelUpQuestion[] = [
  {
    id: 'lu_q51',
    category: 'linux_automation',
    company: 'Google',
    domain: 'Linux Kernel & High-Concurreny I/O',
    topic: 'Linux epoll vs select/poll O(1) Scalability',
    question: 'Why does Linux epoll achieve O(1) event readiness notification scaling to millions of descriptors, whereas select and poll suffer from O(N) performance degradation?',
    options: [
      'epoll uses an in-kernel Red-Black tree to track monitored descriptors and a ready-list doubly linked list populated by device driver interrupt callbacks, returning only ready events to userspace',
      'epoll copies all file descriptor arrays into CPU L1 cache on every syscall',
      'select and poll run as user-space daemons while epoll runs exclusively inside NIC hardware firmware',
      'epoll bypasses the Linux Virtual File System (VFS) and accesses hardware registers directly',
    ],
    correctIndex: 0,
    explanation: 'epoll_create initializes a kernel struct containing a Red-Black tree (for tracking registered FDs) and a ready list. Network card interrupts register callbacks that append ready sockets directly to the ready list. epoll_wait merely inspects this list in O(1) time without scanning all monitored descriptors.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q52',
    category: 'coding',
    company: 'Meta',
    domain: 'Lock-Free Data Structures',
    topic: 'ABA Problem & Hazard Pointers',
    question: 'In lock-free concurrent programming using Compare-And-Swap (CAS), what is the ABA problem, and how do Hazard Pointers prevent it during memory deallocation?',
    options: [
      'A memory address is read as A, updated to B, and restored to A by another thread; CAS succeeds blindly despite state mutation. Hazard Pointers allow reader threads to publish pointers in use, delaying deallocation until no readers hold them',
      'Threads deadlock because mutexes are locked in alphabetical order',
      'CAS operations throw hardware CPU interrupts when integers overflow',
      'Compiler registers lose cache coherency across NUMA nodes',
    ],
    correctIndex: 0,
    explanation: 'The ABA problem occurs when thread 1 reads value A at address P, gets descheduled, thread 2 frees P (value changes to B) and reallocates P with value A. Thread 1\'s CAS(P, A, new_val) succeeds even though the underlying data structure mutated. Hazard Pointers protect memory reclamation until all concurrent readers release their reference.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q53',
    category: 'coding',
    company: 'Apple',
    domain: 'Low-Level Concurrency & Memory Models',
    topic: 'Acquire-Release vs Sequential Consistency Semantics',
    question: 'In the C++11 / Java memory model, how does Acquire-Release memory ordering differ from Sequentially Consistent (seq_cst) ordering?',
    options: [
      'Acquire-Release establishes pairwise synchronization between writing and reading threads without enforcing a single globally observed total order across all independent write operations',
      'Acquire-Release completely disables CPU instruction reordering across the entire operating system',
      'Sequential consistency requires no hardware memory fence instructions on x86 architectures',
      'Acquire-Release only applies to single-core microprocessors',
    ],
    correctIndex: 0,
    explanation: 'memory_order_release guarantees prior memory writes cannot be reordered after the store. memory_order_acquire guarantees subsequent reads cannot be reordered before the load. While synchronizing the communicating threads, Acquire-Release does NOT require a single globally agreed total order of writes seen by all threads, unlike seq_cst.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q54',
    category: 'coding',
    company: 'Microsoft',
    domain: 'Hardware Architecture & Multi-Core Caching',
    topic: 'False Sharing & Cache Line Ping-Pong (MESI)',
    question: 'What hardware phenomenon causes "False Sharing" when two concurrent threads running on distinct CPU cores independently modify separate integer variables located within the same 64-byte cache line?',
    options: [
      'The CPU cache coherence protocol (e.g. MESI) repeatedly invalidates and transfers the entire 64-byte cache line back and forth between core L1 caches, causing massive bus stall penalties',
      'The CPU hardware ALU calculates incorrect arithmetic sums due to shared registers',
      'Operating system kernel threads deadlock in an unrecoverable infinite spin-wait',
      'RAM memory banks overheat and trigger hardware thermal throttling',
    ],
    correctIndex: 0,
    explanation: 'Even if threads access completely distinct variables (e.g. struct { int a; int b; }), because modern CPUs operate on 64-byte cache line granularity, writing to `a` marks core 2\'s cache line as Invalid (under MESI/MOESI). Core 2 must fetch the line again over the interconnect, creating expensive cache line bouncing.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q55',
    category: 'linux_automation',
    company: 'Amazon',
    domain: 'Virtual Memory & Hardware Architecture',
    topic: 'HugePages (2MB / 1GB) Impact on TLB Misses',
    question: 'Why does enabling Transparent Huge Pages (THP) or static HugePages (2MB / 1GB instead of standard 4KB pages) dramatically boost performance in memory-intensive databases (e.g., Redis, PostgreSQL)?',
    options: [
      'It increases the memory coverage of the CPU Translation Lookaside Buffer (TLB) by orders of magnitude, slashing TLB miss rates and multi-level page table walk overheads',
      'It compresses virtual memory data using hardware LZ4 accelerators',
      'It prevents the Linux kernel from allocating memory to background daemons',
      'It doubles the clock speed of physical DDR4/DDR5 memory channels',
    ],
    correctIndex: 0,
    explanation: 'A modern CPU L1 TLB holds ~64-128 entries. With 4KB pages, 64 entries cover only 256 KB of memory, causing frequent TLB misses that require 4-level or 5-level hardware page table walks in RAM. With 2MB HugePages, 64 entries cover 128 MB of memory, practically eliminating page table walk penalties.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q56',
    category: 'linux_automation',
    company: 'Google',
    domain: 'OS Internals & CPU Scheduling',
    topic: 'Linux Context Switch Costs & Pipeline Invalidation',
    question: 'Beyond saving general-purpose CPU registers and switching stack pointers, what constitutes the largest hidden performance cost of an OS thread context switch?',
    options: [
      'Loss of CPU cache warmth (L1/L2 data and instruction caches), TLB invalidation (unless PCID is enabled), and branch predictor state pollution',
      'Writing the entire process memory footprint to the NVMe swap partition',
      'Recalculating network interface socket checksums',
      'Zeroing out all CPU floating-point vector registers',
    ],
    correctIndex: 0,
    explanation: 'The direct cost of saving registers is ~1-2 microseconds. The indirect cost (cache pollution) is much larger: the new thread displaces cache lines from L1/L2 and pollutes branch target buffers. Subsequent execution suffers hundreds of cache misses and pipeline stalls until caches become warm again.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q57',
    category: 'database_platforms',
    company: 'Microsoft',
    domain: 'Database Internals & Storage',
    topic: 'PostgreSQL MVCC Heap Tuple Visibility & VACUUM',
    question: 'In PostgreSQL\'s Multi-Version Concurrency Control (MVCC) engine, how are row updates physically executed, and why is the VACUUM process required?',
    options: [
      'Updates do not overwrite in place; they append a new tuple to the heap table and set xmax on the old tuple. VACUUM is required to reclaim dead tuples and freeze transaction IDs to prevent 32-bit transaction wraparound',
      'Updates overwrite the raw disk block in place and discard previous transactions immediately',
      'PostgreSQL uses in-memory rollback segments identical to Oracle UNDO tablespaces',
      'VACUUM compresses SQL query plans into binary bytecode',
    ],
    correctIndex: 0,
    explanation: 'Postgres uses append-only MVCC: an UPDATE inserts a new physical tuple and stamps the old tuple\'s header with `xmax = current_xid`. Dead tuples remain on disk until `VACUUM` purges them. Moreover, because transaction IDs are 32-bit integers, VACUUM FREEZE is critical to prevent catastrophic 2-billion transaction ID wraparound data loss.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q58',
    category: 'database_platforms',
    company: 'Amazon',
    domain: 'Database Index Engineering',
    topic: 'B+ Tree Fanout & Leaf Page Pointers',
    question: 'Why do relational database storage engines (e.g. InnoDB, Postgres) universally implement B+ Trees with high fanout (100–500) and leaf-node sibling pointers rather than classic B-Trees or binary search trees?',
    options: [
      'High fanout limits tree height to 3–4 levels minimizing disk random I/O seeks; keeping all keys in leaf nodes connected by doubly-linked pointers enables ultra-fast O(1) sequential range scans',
      'B+ Trees store all actual table data inside root nodes for instantaneous primary key lookups',
      'Binary trees require less CPU memory than B+ trees on SSD storage devices',
      'B+ Trees automatically encrypt data blocks using hardware TPM modules',
    ],
    correctIndex: 0,
    explanation: 'On disk/SSD, random page lookups cost milliseconds or microseconds. A B+ Tree with page size 16KB and fanout 200 indexes 8 million rows in just 3 levels (3 disk reads). Interior nodes contain only routing keys, maximizing fanout. Leaf nodes form a doubly linked list, allowing range queries (BETWEEN x AND y) without traversing parent branches.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q59',
    category: 'database_platforms',
    company: 'Google',
    domain: 'Database Crash Recovery',
    topic: 'ARIES Recovery Algorithm Three-Phase Sequence',
    question: 'What is the correct sequential execution order of the three phases in the classic C. Mohan ARIES database crash recovery algorithm?',
    options: [
      'Analysis Phase -> Redo Phase (Repeating History) -> Undo Phase (Rolling back active loser transactions)',
      'Undo Phase -> Redo Phase -> Analysis Phase',
      'Redo Phase -> Analysis Phase -> Checkpoint Phase',
      'Snapshot Phase -> Garbage Collection -> Index Rebuild',
    ],
    correctIndex: 0,
    explanation: 'ARIES recovers in 3 phases: (1) Analysis: scans log forward from last checkpoint to determine dirty pages and active transactions at crash time. (2) Redo: "repeats history" forward, reapplying all logged operations (even for aborted transactions) to return DB to crash state. (3) Undo: scans backward undoing actions of uncommitted "loser" transactions.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q60',
    category: 'database_platforms',
    company: 'Meta',
    domain: 'Concurrency Control & Deadlocks',
    topic: 'Wound-Wait vs Wait-Die Deadlock Prevention',
    question: 'In timestamp-based deadlock prevention protocols, how does the "Wound-Wait" preemptive algorithm handle a conflict when an older transaction T_old requests a lock held by a younger transaction T_young?',
    options: [
      'T_old "wounds" (preempts and aborts) T_young, forcing T_young to roll back and release the lock immediately',
      'T_old is forced to die and roll back while T_young continues execution',
      'Both transactions wait indefinitely until a human DBA terminates the session',
      'The database converts both transactions to READ UNCOMMITTED isolation level',
    ],
    correctIndex: 0,
    explanation: 'In Wound-Wait: if older requests lock held by younger, older "wounds" younger (younger aborts and restarts with its original timestamp). If younger requests lock held by older, younger is permitted to wait. Wound-Wait minimizes aborts because older transactions preempt younger ones immediately.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q61',
    category: 'linux_automation',
    company: 'Netflix',
    domain: 'Linux Kernel CPU Scheduling',
    topic: 'CFS (Completely Fair Scheduler) vruntime Tracking',
    question: 'In the Linux Completely Fair Scheduler (CFS), how is a task\'s `vruntime` (virtual runtime) updated, and which task does CFS select next to execute on a CPU core?',
    options: [
      'vruntime increases proportional to physical execution time scaled inversely by the task\'s nice priority weight; CFS always picks the task with the minimum vruntime (leftmost node in the Red-Black tree)',
      'vruntime counts the number of network packets received per second; the task with highest vruntime executes first',
      'CFS executes tasks in strict First-In-First-Out (FIFO) queue order',
      'Tasks with negative nice values have their vruntime reset to zero every second',
    ],
    correctIndex: 0,
    explanation: 'CFS models an "ideal multi-tasking CPU". Each task has a virtual runtime: vruntime += delta_exec * (NICE_0_LOAD / task_weight). Higher priority (lower nice) tasks have larger weights, causing their vruntime to advance slower. CFS always schedules the task with the lowest vruntime (the leftmost node in its RB-tree).',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q62',
    category: 'coding',
    company: 'Meta',
    domain: 'Memory Management & Allocators',
    topic: 'jemalloc / tcmalloc Thread Caching Architecture',
    question: 'Why do high-performance web servers (e.g. Nginx, Envoy, Node.js) replace the glibc ptmalloc allocator with jemalloc or tcmalloc in multi-threaded production workloads?',
    options: [
      'They utilize thread-local caches (tcache) and arena-based partitioning, practically eliminating global lock contention on malloc/free and drastically reducing virtual memory fragmentation',
      'They disable virtual memory paging and enforce physical memory locking via mlockall',
      'They compress C++ pointers from 64-bit to 16-bit using custom x86 microcode',
      'They automatically garbage-collect dangling raw pointers without compiler support',
    ],
    correctIndex: 0,
    explanation: 'Standard allocators suffer from lock contention when hundreds of threads call malloc/free simultaneously. tcmalloc and jemalloc allocate thread-local caches (tcache) for small size classes. Most allocations happen without acquiring any lock, and arenas partition heap space to combat memory fragmentation.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q63',
    category: 'linux_automation',
    company: 'Netflix',
    domain: 'Linux Kernel & High-Performance I/O',
    topic: 'Zero-Copy sendfile() & splice() Architecture',
    question: 'When serving high-bitrate video streams from disk to network sockets, how does the Linux `sendfile()` system call achieve "Zero-Copy" compared to traditional `read()` and `write()` loops?',
    options: [
      'Data is transferred directly from the OS page cache to the network socket buffer via DMA without copying bytes into user-space application memory buffers or triggering user-kernel context copy overhead',
      'Video files are encoded into raw IP packets directly on the NVMe disk controller firmware',
      'Data bypasses the Linux network stack and is transmitted via raw PCIe bus broadcasting',
      'The web server executes directly in kernel ring 0 supervisor mode',
    ],
    correctIndex: 0,
    explanation: 'Traditional read/write requires 4 context switches and 4 copies (Disk -> Page Cache -> User Buffer -> Socket Buffer -> NIC). With sendfile() and scatter-gather DMA, data transfers directly from page cache to NIC descriptor buffers without ever copying bytes into user space, slashing CPU load and memory bus pressure.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q64',
    category: 'linux_automation',
    company: 'Apple',
    domain: 'OS Memory Virtualization',
    topic: 'Copy-on-Write (COW) fork() Page Table Manipulation',
    question: 'When a process invokes the `fork()` system call in Linux, how does the kernel avoid immediately copying gigabytes of physical memory to the child process?',
    options: [
      'It duplicates only the parent\'s page table entries and marks all physical pages as READ-ONLY; when either process writes to a page, a CPU page fault interrupts the kernel to allocate and copy only that specific 4KB page',
      'It compresses parent memory into a temporary ZRAM block device',
      'It shares all memory with read-write permissions without tracking write events',
      'The child process runs inside a hardware hypervisor container',
    ],
    correctIndex: 0,
    explanation: 'Copy-on-Write marks all physical pages of the parent and child as read-only and increments their reference count. When either parent or child executes a write instruction, the CPU MMU triggers a Page Fault. The kernel traps this, allocates a new physical frame, copies the 4KB page, and updates that process\'s page table.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q65',
    category: 'coding',
    company: 'Microsoft',
    domain: 'Hardware Architecture & Microbenchmarking',
    topic: 'Branch Predictor Misprediction Penalty',
    question: 'Why does sorting an array before iterating through a loop with an `if (arr[i] > 128)` branch result in a 3x to 6x speedup on modern superscalar processors?',
    options: [
      'The CPU branch target buffer and two-level adaptive branch predictor achieve near 100% prediction accuracy, preventing catastrophic pipeline flushes that discard 15–20 speculative execution stages',
      'Sorted arrays fit into smaller CPU registers than unsorted arrays',
      'The compiler completely removes the `if` statement during bytecode optimization',
      'Unsorted arrays trigger hardware page faults on every index jump',
    ],
    correctIndex: 0,
    explanation: 'Modern CPUs have deep pipelines (15-20 stages). On random unsorted data, the branch outcome is 50/50, causing the branch predictor to fail frequently. Each misprediction flushes all speculatively executed instructions in the pipeline. On sorted data, the branch outcome is predictable (all false, then all true), keeping pipelines full.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q66',
    category: 'linux_automation',
    company: 'Google',
    domain: 'Linux Synchronization Primitives',
    topic: 'Futex (Fast Userspace Mutex) Architecture',
    question: 'How does a Linux `futex` achieve near-zero overhead synchronization when locks are uncontended?',
    options: [
      'Uncontended acquisition and release execute purely in user space via atomic CAS instructions without issuing a single system call; the kernel is invoked via `sys_futex()` only when contention requires sleeping or waking threads',
      'By running all synchronization inside the CPU level-1 cache memory controller',
      'By turning all multi-threaded applications into single-threaded coroutines',
      'By utilizing spinlocks exclusively and disabling thread sleep entirely',
    ],
    correctIndex: 0,
    explanation: 'Before futexes, acquiring a mutex required entering the kernel via a syscall. A futex uses an integer in user-space memory: uncontended lock/unlock is a single atomic CAS (0 syscalls, ~5 nanoseconds). Only if the CAS detects contention does the thread invoke `futex(val, FUTEX_WAIT)` to let the kernel put it to sleep.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q67',
    category: 'coding',
    company: 'Meta',
    domain: 'Concurrency & Language Runtimes',
    topic: 'Stackless vs Stackful Coroutines (Go vs Node/Rust)',
    question: 'What is the architectural difference between Stackful coroutines (e.g. Go goroutines) and Stackless coroutines (e.g. JavaScript async/await, Rust/C++20 coroutines)?',
    options: [
      'Stackful coroutines allocate their own resizable runtime call stack and can suspend from deeply nested helper functions; Stackless coroutines compile into state-machine structs and can only suspend at the top-level yield/await frame',
      'Stackful coroutines cannot run on multi-core processors',
      'Stackless coroutines require POSIX pthread context switching for every function call',
      'Stackful coroutines consume zero bytes of heap memory',
    ],
    correctIndex: 0,
    explanation: 'Go goroutines are stackful: each starts with a small ~2KB stack and can pause inside any nested function call. Stackless coroutines (Rust/C++/JS) do not have a dedicated stack; the compiler transforms the async function into a state machine struct storing local variables on the heap, suspending only at explicit await points.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q68',
    category: 'database_platforms',
    company: 'Microsoft',
    domain: 'Relational Database ACID Theory',
    topic: 'Write Skew Anomaly under Snapshot Isolation',
    question: 'In database isolation theory, which concurrency anomaly can occur under Snapshot Isolation (SI) that is strictly prevented only by Serializable isolation?',
    options: [
      'Write Skew (where two concurrent transactions read overlapping data sets satisfying an invariant, but write to disjoint rows such that the overall invariant is violated)',
      'Dirty Read (reading uncommitted data from another transaction)',
      'Lost Update on a single row (two transactions overwriting the same primary key)',
      'Non-repeatable read of an individual integer column',
    ],
    correctIndex: 0,
    explanation: 'Snapshot Isolation guarantees that each transaction sees a consistent snapshot of the database, preventing dirty reads and non-repeatable reads. However, it is vulnerable to Write Skew: e.g. two on-call doctors simultaneously look at active doctors (>1) and unregister themselves. Both writes succeed, leaving 0 doctors on-call.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q69',
    category: 'database_platforms',
    company: 'Amazon',
    domain: 'Database Index Engineering',
    topic: 'B+ Tree Lock Coupling (Crabbing) Protocol',
    question: 'In the Lock Coupling (Crabbing) concurrency protocol for B+ Tree traversal, when can a thread safely release the lock on a parent node during an INSERT operation?',
    options: [
      'Only after acquiring the lock on child node C AND verifying that C is "safe" (i.e. C is not full and will not trigger a cascading split back to the parent)',
      'Immediately before attempting to look up the child node in memory',
      'Only after the entire database transaction commits to the write-ahead log',
      'Parent locks are never released until the server reboots',
    ],
    correctIndex: 0,
    explanation: 'In lock crabbing: acquire parent lock, acquire child lock. If the child is "safe" (cannot split on insert, or cannot underflow on delete), the child will not propagate changes upward. Thus, the parent lock can be safely released. If the child is full, the parent lock must be retained in case a node split is required.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q70',
    category: 'linux_automation',
    company: 'Google',
    domain: 'Linux Container Internals',
    topic: 'Linux Namespaces vs cgroups Container Mechanics',
    question: 'In the Linux container runtime architecture (e.g. Docker, containerd), what distinct roles do Kernel Namespaces and Control Groups (cgroups) play?',
    options: [
      'Namespaces provide Isolation (what a container can SEE: PID, NET, MNT, IPC, UTS, USER); cgroups provide Resource Metering & Limitation (what a container can USE: CPU shares, Memory limits, I/O bandwidth)',
      'Namespaces allocate physical RAM; cgroups manage network routing tables',
      'Namespaces are implemented in userspace; cgroups run inside hardware virtualization chips',
      'Both perform identical cryptographic functions for TLS certificates',
    ],
    correctIndex: 0,
    explanation: 'A container is not a real standalone entity in the Linux kernel; it is a standard Linux process isolated by Namespaces (restricting visibility so PID 1 inside the container is PID 12432 on the host) and bounded by cgroups (enforcing CFS CPU quotas, memory limits, and invoking the OOM killer if memory limits are breached).',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q71',
    category: 'linux_automation',
    company: 'Apple',
    domain: 'Virtual Memory & Page Eviction',
    topic: 'Clock Algorithm (Second Chance) Page Replacement',
    question: 'How does the Clock (Second-Chance) algorithm approximate Least Recently Used (LRU) page replacement in modern OS kernel memory managers with low CPU overhead?',
    options: [
      'It maintains a circular list of frames with a reference bit; the hand advances clearing reference bits (giving a "second chance") and selects the first frame encountered whose reference bit is already 0',
      'It tracks 64-bit microsecond timestamps for every memory read instruction in hardware',
      'It evicts the largest memory page regardless of access frequency',
      'It randomly terminates the process with the highest memory footprint',
    ],
    correctIndex: 0,
    explanation: 'True LRU requires moving nodes in a doubly-linked list on EVERY memory access (unacceptable lock overhead). The Clock algorithm uses the hardware MMU reference bit (set to 1 automatically by CPU on read/write). When memory pressure occurs, the clock hand sweeps: if bit is 1, it sets it to 0; if bit is 0, it evicts that page.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q72',
    category: 'linux_automation',
    company: 'Netflix',
    domain: 'Storage Subsystems & NVMe',
    topic: 'NVMe Multi-Queue (blk-mq) Linux Architecture',
    question: 'Why did Linux introduce the multiqueue block layer (blk-mq) replacing the legacy single-queue I/O scheduler for modern NVMe solid-state drives?',
    options: [
      'Modern NVMe SSDs support up to 64,000 queues with 64,000 commands each; blk-mq maps CPU core-local queues to hardware queues, eliminating the single global request-queue spinlock bottleneck',
      'Legacy schedulers did not support files larger than 2 GB',
      'blk-mq runs exclusively on spinning magnetic hard drives to reduce head seek times',
      'NVMe drives require all data to be written as uncompressed text strings',
    ],
    correctIndex: 0,
    explanation: 'Legacy Linux block layers had a single request queue protected by a global spinlock, which choked when SSDs started delivering 1,000,000+ IOPS. blk-mq provides a two-stage queue: software staging queues per CPU core (zero lock contention) mapped to hardware dispatch queues on the NVMe controller.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q73',
    category: 'coding',
    company: 'Uber',
    domain: 'Garbage Collection & Memory Runtimes',
    topic: 'Concurrent Marking & Tri-Color Write Barriers',
    question: 'In concurrent garbage collection algorithms (e.g. Go GC, Java ZGC), why is a "Write Barrier" mandatory while user mutator threads run concurrently with GC marking threads?',
    options: [
      'To prevent the "Lost Object" bug: if a black object is modified to point to a white object while the only path from gray objects is severed, the white object would be incorrectly collected as garbage',
      'To prevent threads from writing integer values larger than 64 bits',
      'To force the operating system to flush CPU L3 cache to physical RAM on every pointer write',
      'To encrypt pointer addresses against Spectre side-channel attacks',
    ],
    correctIndex: 0,
    explanation: 'Tri-color marking uses White (unvisited), Gray (visited, children unvisited), Black (visited, children visited). The strong tri-color invariant forbids pointers from Black directly to White without a Gray intermediate. A write barrier intercepts pointer mutations, coloring newly linked white objects gray so they are not lost.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q74',
    category: 'linux_automation',
    company: 'Google',
    domain: 'Linux Kernel Synchronization',
    topic: 'Read-Copy-Update (RCU) Grace Period Mechanics',
    question: 'In the Linux kernel, how does Read-Copy-Update (RCU) allow reader threads to read shared data structures with ZERO lock overhead, zero atomic instructions, and zero cache-line invalidation?',
    options: [
      'Writers copy the data, mutate the copy, atomically swap the pointer, and defer freeing old memory until all concurrent reader threads pass through a "quiescent state" (Grace Period)',
      'Readers disable kernel hardware interrupts for the entire duration of the read',
      'All data structures are replicated across every CPU core register file',
      'RCU converts all pointer reads into non-blocking UDP socket transmissions',
    ],
    correctIndex: 0,
    explanation: 'RCU readers simply enter rcu_read_lock() (which on non-preempt kernels compiles to literally nothing). Writers create a copy of the struct, update it, and atomically update the pointer. The writer then waits for a Grace Period—until every CPU core has undergone a context switch—guaranteeing all old readers finished before freeing.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q75',
    category: 'coding',
    company: 'Microsoft',
    domain: 'Compiler Optimization & SIMD',
    topic: 'SIMD AVX-512 Vectorization & Memory Alignment',
    question: 'When optimizing high-throughput matrix multiplication or vector similarity search with Intel AVX-512 vector instructions, why must memory pointers be aligned to 64-byte boundaries?',
    options: [
      'Aligned vector load instructions (`_mm512_load_ps`) avoid expensive split-line cache access penalties and pipeline stalls that occur when a 512-bit register load spans across two separate 64-byte cache lines',
      'Unaligned memory addresses trigger immediate hardware CPU core shutdowns',
      'AVX-512 cannot process floating point numbers smaller than 1.0',
      'Memory alignment automatically doubles the clock frequency of the CPU',
    ],
    correctIndex: 0,
    explanation: 'A 512-bit register holds 64 bytes (16 single-precision floats). If an array address is not 64-byte aligned, loading 64 bytes crosses two separate L1 cache lines, forcing the memory execution engine to perform two cache lookups, merge the bytes in a temporary buffer, and stall pipeline throughput.',
    difficulty: 'Hard',
  },
];
