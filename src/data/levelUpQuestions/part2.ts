import { LevelUpQuestion } from './part1';

export const LEVEL_UP_PART_2: LevelUpQuestion[] = [
  {
    id: 'lu_q26',
    category: 'cloud_platform',
    company: 'Amazon',
    domain: 'Distributed Consensus & Fault Tolerance',
    topic: 'Raft Leader Election Split Vote Mitigation',
    question: 'In the Raft distributed consensus algorithm, how does Raft guarantee that split votes during leader election are rapidly and deterministically resolved without livelock?',
    options: [
      'Nodes utilize randomized election timeouts chosen uniformly from a range (e.g., 150–300ms), ensuring one candidate times out and collects a majority first',
      'The cluster selects a coordinator using deterministic node ID lexicographical ordering',
      'Candidates negotiate quorum through an external ZooKeeper ensemble',
      'Term numbers are incremented by arbitrary powers of two until parity is achieved',
    ],
    correctIndex: 0,
    explanation: 'Raft uses randomized election timeouts (e.g., 150-300ms) to prevent split-vote livelocks. Randomization spreads out node timeouts so that usually only a single follower times out, transitions to candidate, and collects majority votes before others wake up.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q27',
    category: 'database_platforms',
    company: 'Google',
    domain: 'Distributed Consensus & ACID Engines',
    topic: 'Multi-Paxos Phase 1 Elimination',
    question: 'In Multi-Paxos, under steady-state operation with a stable leader, how is message overhead reduced compared to standard Basic Paxos?',
    options: [
      'Phase 1 (Prepare/Promise) is executed only once per leader term; subsequent log entries require only Phase 2 (Accept/Accepted) round trips',
      'Phase 2 is eliminated entirely by relying on NTP synchronized wall clocks',
      'All replicas acknowledge log entries asynchronously without requiring a majority quorum',
      'Write proposals are sent only to a single backup node rather than the entire cluster',
    ],
    correctIndex: 0,
    explanation: 'Basic Paxos requires two full round-trips for every committed value (Prepare/Promise followed by Accept/Accepted). Multi-Paxos combines Phase 1 for all future log entries into a single election phase. Once a leader is established, only Phase 2 is executed per log entry.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q28',
    category: 'cloud_platform',
    company: 'Amazon',
    domain: 'Distributed Systems Theory',
    topic: 'PACELC Theorem Systems Classification',
    question: 'According to Daniel Abadi\'s PACELC theorem, how are MongoDB (with majority write concern and primary reads) and Amazon DynamoDB (with default settings) classified when there is NO partition (the ELC trade-off)?',
    options: [
      'MongoDB is PC/EC (Consistent); DynamoDB is PA/EL (Latency over Consistency)',
      'MongoDB is PA/EL; DynamoDB is PC/EC',
      'Both are classified strictly as PA/EC systems',
      'Both discard availability in all operating conditions',
    ],
    correctIndex: 0,
    explanation: 'PACELC states: if Partition (P), choose Availability (A) or Consistency (C); Else (E), trade Latency (L) or Consistency (C). With majority writes and primary reads, MongoDB is PC/EC (prioritizing Consistency). DynamoDB default reads prioritize lower Latency over Consistency (PA/EL).',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q29',
    category: 'database_platforms',
    company: 'Microsoft',
    domain: 'Atomic Distributed Commits',
    topic: 'Two-Phase Commit (2PC) Coordinator Failure',
    question: 'What is the fundamental architectural limitation of Two-Phase Commit (2PC) that causes it to be a blocking protocol?',
    options: [
      'If the coordinator crashes after participants vote "YES" in Phase 1 but before broadcasting "COMMIT/ABORT", participants cannot safely decide and must block indefinitely',
      'Participants must restart their operating system kernel before acknowledging commits',
      'It cannot support transactions involving more than two distinct database partitions',
      'Network latency increases exponentially with the number of table indices',
    ],
    correctIndex: 0,
    explanation: 'In 2PC, once participants vote "YES" in the prepare phase, they hold locks on transactional resources. If the coordinator crashes before issuing Commit or Abort, participants cannot unilateral abort (the coordinator might have decided Commit) or commit (it might have decided Abort), blocking indefinitely.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q30',
    category: 'cloud_platform',
    company: 'Netflix',
    domain: 'Distributed Systems & Ordering',
    topic: 'Vector Clocks vs Lamport Timestamps',
    question: 'Why can Lamport Timestamps determine total ordering of events but CANNOT be used to detect whether two events are casually concurrent?',
    options: [
      'Lamport Timestamps enforce A -> B => L(A) < L(B), but L(A) < L(B) does NOT imply A caused B; Vector Clocks capture causal history per node enabling exact concurrency detection',
      'Lamport clocks rely on millisecond hardware crystals which suffer from thermal clock drift',
      'Vector clocks only support up to two concurrent nodes before integer overflow',
      'Lamport timestamps can only be used in peer-to-peer networks without master nodes',
    ],
    correctIndex: 0,
    explanation: 'Lamport timestamps satisfy the clock condition (if a -> b then L(a) < L(b)), providing a total order consistent with causality. However, the converse does not hold: L(a) < L(b) does not tell you whether a happened before b or if they were concurrent. Vector clocks provide an iff relationship.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q31',
    category: 'cloud_platform',
    company: 'Amazon',
    domain: 'Consistent Hashing & Partitioning',
    topic: 'Virtual Nodes (Vnodes) Variance Minimization',
    question: 'In a Consistent Hashing ring (as used in Amazon Dynamo), why are multiple "virtual nodes" (vnodes) assigned to each physical server instead of a single point on the hash ring?',
    options: [
      'To prevent non-uniform partition sizing and load skew, and to enable proportional rebalancing across remaining nodes during physical machine failures',
      'To encrypt key identifiers using SHA-256 before disk writes',
      'To bypass TCP maximum transmission unit (MTU) limitations during intra-cluster RPCs',
      'To ensure hardware RAM pages align with CPU cache lines',
    ],
    correctIndex: 0,
    explanation: 'With a single token per machine, random hash distribution causes high variance in key ownership (O(1/N) load variance). Vnodes (e.g. 128 or 256 per node) break the ring into smaller segments, achieving near-uniform load distribution and distributing rebalance load evenly among all remaining nodes on failure.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q32',
    category: 'cloud_platform',
    company: 'Netflix',
    domain: 'Peer-to-Peer & Anti-Entropy',
    topic: 'Merkle Tree Anti-Entropy Exchange',
    question: 'How do distributed key-value stores (such as Apache Cassandra and Amazon Dynamo) use Merkle Trees during background anti-entropy repairs to minimize cross-datacenter network bandwidth?',
    options: [
      'Replicas compare root hashes; if matching, ranges are identical. If different, they recursively descend branches, transferring data only for mismatched leaf ranges',
      'Nodes stream entire raw SSTables over gRPC and perform byte-by-byte diffs',
      'Merkle trees compress payloads using LZ4 before TLS serialization',
      'A centralized coordinator broadcasts hash checksums every 10 milliseconds',
    ],
    correctIndex: 0,
    explanation: 'A Merkle tree is a binary hash tree of keys and values. Two replicas compare the root hash of a partition range. If identical, the data is guaranteed in sync. If different, they compare child hashes recursively, identifying exactly the out-of-sync sub-ranges and streaming only the differing rows.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q33',
    category: 'cloud_platform',
    company: 'Uber',
    domain: 'Distributed Consistency Models',
    topic: 'Read-After-Write (Monotonic Read) Guarantees',
    question: 'In an asynchronous primary-replica architecture with high read traffic, how can an application guarantee Read-After-Write consistency for the author of an update without routing all reads to the primary database?',
    options: [
      'Pin the author\'s user session to read from the primary for a duration exceeding replication lag, or track the update\'s Log Sequence Number (LSN) and read only from replicas caught up to that LSN',
      'Disable database indexes on replica nodes to accelerate replication speed',
      'Force client browsers to cache the entire database row in IndexedDB indefinitely',
      'Downgrade database write isolation to READ UNCOMMITTED',
    ],
    correctIndex: 0,
    explanation: 'Read-your-writes consistency can be achieved by: (1) routing only the updating user\'s reads to the primary for a window exceeding max replication lag (e.g. 10 seconds), or (2) passing the commit LSN/version in a client session token and querying replicas only when their replication replay position >= commit LSN.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q34',
    category: 'cloud_platform',
    company: 'Amazon',
    domain: 'Microservices & Distributed Transactions',
    topic: 'Saga Pattern: Choreography vs Orchestration',
    question: 'In an eCommerce checkout workflow involving Order, Payment, and Inventory microservices, what is the primary risk of using Choreography-based Sagas compared to Orchestration-based Sagas?',
    options: [
      'Choreography introduces implicit event-coupling, risk of cyclic event dependencies, and high difficulty in tracking workflow state or debugging distributed compensating failures',
      'Choreography requires a single point of failure coordinator database',
      'Choreography cannot use message brokers such as Apache Kafka or AWS SQS',
      'Orchestration inherently violates ACID properties whereas choreography guarantees strict serializability',
    ],
    correctIndex: 0,
    explanation: 'Choreography relies on services reacting to each other\'s events. As workflows grow, circular dependencies, race conditions in compensating transactions, and lack of a centralized status view make system understanding and operational debugging exceedingly difficult compared to Orchestration.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q35',
    category: 'cloud_platform',
    company: 'Apple',
    domain: 'Eventual Consistency & CRDTs',
    topic: 'State-based (CvRDT) vs Operation-based (CmRDT) CRDTs',
    question: 'What mathematical algebraic structure must the merge function f(x, y) of a State-based Conflict-Free Replicated Data Type (CvRDT) satisfy to guarantee convergence under out-of-order and duplicated message delivery?',
    options: [
      'A bounded join-semilattice where merge is Commutative, Associative, and Idempotent: f(x, x) = x',
      'A non-abelian Lie group with continuous differentiability',
      'A ring homomorphism over modular Galois fields',
      'A monotonic vector space with strictly positive eigenvalues',
    ],
    correctIndex: 0,
    explanation: 'A CvRDT states that all local states form a join-semilattice equipped with partial order <=. The merge operation computes the least upper bound (join). For convergence over unreliable networks, the merge operator must be Commutative (order-independent), Associative (grouping-independent), and Idempotent (safe against duplicates).',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q36',
    category: 'cloud_platform',
    company: 'Stripe',
    domain: 'High-Throughput API Infrastructure',
    topic: 'Distributed Sliding Window Rate Limiting',
    question: 'Why is the Sliding Window Counter algorithm preferred over Fixed Window and Token Bucket algorithms in distributed high-frequency payment gateways?',
    options: [
      'It prevents burst spikes of up to 2x the limit at window boundaries while maintaining constant memory footprint using weighted sums of current and previous window counts',
      'It eliminates the need for atomic storage engines like Redis or Memcached',
      'It operates purely client-side without any server coordination',
      'It allows infinite burst capacity during network outages',
    ],
    correctIndex: 0,
    explanation: 'Fixed window algorithms suffer from boundary bursts (e.g. 100 req at 00:59 and 100 req at 01:00 = 200 req in 2 seconds). Sliding window log requires storing timestamps (high memory). Sliding window counter estimates current requests using: prev_count * ((window - elapsed) / window) + curr_count, preventing spikes with O(1) memory.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q37',
    category: 'cloud_platform',
    company: 'Google',
    domain: 'High-Availability Clustering',
    topic: 'Fencing Tokens for Split-Brain Protection',
    question: 'When a distributed master node experiences a garbage collection pause and loses its lock, why is an explicit "Fencing Token" required to prevent the old master from corrupting shared storage after waking up?',
    options: [
      'Locks can expire before the client completes execution; a monotonically increasing fencing token ensures the storage service rejects writes bearing a stale, lower token number',
      'Fencing tokens encrypt data blocks using ephemeral AES-GCM session keys',
      'The storage engine must verify the process ID (PID) of the writing Linux kernel',
      'TCP RST packets must be broadcast across all network interface cards',
    ],
    correctIndex: 0,
    explanation: 'As highlighted in Martin Kleppmann\'s analysis, distributed locks cannot guarantee mutual exclusion if a client holds a lock, pauses (GC or network stall), the lock expires and is acquired by another node. A monotonically increasing fencing token sent with each write allows storage to reject obsolete writes from the paused node.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q38',
    category: 'cloud_platform',
    company: 'Netflix',
    domain: 'Distributed Caching & Concurrency',
    topic: 'Thundering Herd (Cache Stampede) Mitigation',
    question: 'When a popular cached video catalog entry expires under 50,000 requests/second, what mechanism prevents the "Thundering Herd" (Cache Stampede) problem from overloading the database?',
    options: [
      'Probabilistic early expiration (XFetch algorithm) or a singleflight/mutex lock ensuring only one thread regenerates the cache while others wait or serve stale data',
      'Increasing Redis memory eviction limit to maxmemory-noeviction',
      'Disabling database connection pooling to throttle incoming traffic',
      'Purging all replica read nodes from the load balancer rotation',
    ],
    correctIndex: 0,
    explanation: 'The XFetch optimal probabilistic cache algorithm computes early recomputation before expiration based on read frequency and computation time (-beta * delta * ln(random()) > TTL - now). Alternatively, a distributed mutex or singleflight pattern ensures exactly one request hits the database while others wait or read stale.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q39',
    category: 'cloud_platform',
    company: 'Netflix',
    domain: 'Fault Tolerance & Resilience',
    topic: 'Circuit Breaker State Machine & Hystrix Patterns',
    question: 'In the Circuit Breaker pattern (e.g. Netflix Hystrix / Resilience4j), when a circuit moves from OPEN to HALF-OPEN state, how does it evaluate downstream health?',
    options: [
      'It permits a limited, configurable probe batch of requests to pass through; if all succeed, the circuit resets to CLOSED, otherwise it immediately trips back to OPEN',
      'It immediately floods downstream services with maximum traffic to test stress limits',
      'It terminates all client TCP connections and redirects users to a 404 page',
      'It executes a rolling reboot of all downstream microservice pods',
    ],
    correctIndex: 0,
    explanation: 'After an OPEN timeout period elapses, the circuit breaker enters HALF-OPEN. In this state, it allows a small trial number of requests to execute against the failing dependency. If they succeed, it assumes recovery and closes the circuit; if any fail, it re-opens the circuit for another backoff interval.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q40',
    category: 'cloud_platform',
    company: 'Stripe',
    domain: 'Distributed Locks & Synchronization',
    topic: 'Redlock Clock Drift Vulnerability Analysis',
    question: 'What is the core critique of the Redis Redlock algorithm when operating in non-formal distributed environments without synchronized hardware clocks?',
    options: [
      'Unsynchronized wall-clock jumps (NTP stepping) and process pauses (GC) can violate lock validity times across independent Redis nodes, allowing multiple clients to hold the lock concurrently',
      'Redis does not support single-threaded event loop execution',
      'Redlock requires an odd number of master nodes exceeding 25 instances',
      'TCP handshake latency causes Redis nodes to corrupt memory segments',
    ],
    correctIndex: 0,
    explanation: 'As demonstrated by distributed systems researchers, Redlock relies on the assumption of bounded clock drift and bounded network/process delays. If NTP abruptly steps time forward on a node, or if a client experiences a stop-the-world GC pause, the lock lease expires unnoticed, leading to concurrent critical section execution.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q41',
    category: 'database_platforms',
    company: 'Uber',
    domain: 'Streaming & Change Data Capture (CDC)',
    topic: 'CDC via Database Write-Ahead Log (WAL)',
    question: 'Why is log-based Change Data Capture (e.g., Debezium tailing MySQL binlog or Postgres WAL) superior to query-based polling (SELECT WHERE updated_at > t)?',
    options: [
      'Log-based CDC captures DELETE operations, intermediate updates within the same transaction, and produces zero additional read load on table indexes',
      'Query-based polling cannot connect via TCP/IP sockets',
      'Log-based CDC bypasses database user authentication permissions',
      'Query-based polling requires all database tables to be stored in memory',
    ],
    correctIndex: 0,
    explanation: 'Query polling misses intermediate updates between polling cycles, cannot detect hard DELETEs (rows vanished without an updated_at timestamp), creates heavy indexing read overhead, and fails on long-running transactions. Log-based CDC reads the committed WAL sequentially with zero table query lock contention.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q42',
    category: 'cloud_platform',
    company: 'Netflix',
    domain: 'Event Streaming & Messaging',
    topic: 'Apache Kafka Exactly-Once Semantics (EOS)',
    question: 'How does Apache Kafka achieve Exactly-Once Semantics (EOS) across a read-process-write pipeline involving consumer groups and topic partitions?',
    options: [
      'Using transactional coordinator state markers, idempotent producer sequence numbers, and committing consumer offsets within the atomic two-phase producer transaction',
      'By deduplicating messages using in-memory LRU caches on every consumer node',
      'By enforcing a single consumer partition per Kafka broker cluster',
      'By disabling broker disk writes and operating entirely in RAM',
    ],
    correctIndex: 0,
    explanation: 'Kafka EOS combines: (1) Idempotent producers with PID and monotonic sequence numbers per partition to prevent duplicate writes, and (2) Transactional markers written to the transaction log, allowing messages and consumer offsets to be committed or aborted atomically across multiple partitions via a 2PC coordinator.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q43',
    category: 'cloud_platform',
    company: 'Google',
    domain: 'Service Mesh & Cloud Infrastructure',
    topic: 'Envoy Proxy Architecture & Threading Model',
    question: 'How does the Envoy proxy achieve high throughput and low tail latency without kernel context switching bottlenecks when handling hundreds of thousands of concurrent connections?',
    options: [
      'A single-process, multi-threaded event-driven architecture utilizing non-blocking libevent loops with thread-local worker instances and no cross-thread lock contention on the data path',
      'Spawning a separate Linux POSIX thread for every incoming HTTP connection',
      'Running as a Linux kernel module inside Netfilter iptables',
      'Executing user-space Java bytecode compiled via GraalVM Native Image',
    ],
    correctIndex: 0,
    explanation: 'Envoy uses a single process with a master thread and multiple worker threads. Each worker runs a non-blocking event loop (using epoll/kqueue) managing its own set of connections independently. Data path execution requires zero cross-thread locking, eliminating mutex contention and maximizing CPU cache locality.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q44',
    category: 'database_platforms',
    company: 'Meta',
    domain: 'Database Partitioning & Sharding',
    topic: 'Scatter-Gather Query Latency Bounds',
    question: 'In a horizontally sharded database with 1,000 shards, what happens to the 99.9th percentile (p999) response latency of a scatter-gather query that must query all 1,000 shards before returning?',
    options: [
      'The query latency is bounded by the slowest single shard response among the 1,000 nodes, causing p999 overall latency to degrade drastically toward the individual shard worst-case',
      'The overall latency equals the mathematical average of all shard response times',
      'The latency decreases because 1,000 CPUs process the query in parallel',
      'The query terminates after the first shard returns its local result set',
    ],
    correctIndex: 0,
    explanation: 'Because a scatter-gather query cannot return until the last shard responds, overall latency is the maximum of 1,000 independent random latency variables. Even if each shard has only a 1% chance of a slow response, the probability that ALL 1,000 shards respond quickly is (0.99)^1000 ≈ 0.000043 (near zero).',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q45',
    category: 'cloud_platform',
    company: 'Amazon',
    domain: 'Message Queuing Architecture',
    topic: 'Pull-based (Kafka) vs Push-based (RabbitMQ) Backpressure',
    question: 'Why does a Pull-based consumer model (as used in Apache Kafka) provide superior backpressure handling compared to a Push-based broker model during sudden downstream consumer overload?',
    options: [
      'Consumers request records at their own processing capacity and rate; if a consumer slows down, records remain buffered safely on broker sequential disk storage without crashing consumers',
      'Pull-based consumers can alter the message payload during the network transfer',
      'Push-based brokers delete messages immediately if network buffers exceed 1 MB',
      'Pull-based architectures avoid using TCP connections entirely',
    ],
    correctIndex: 0,
    explanation: 'In push systems, brokers push messages to consumers, requiring complex prefetch window tuning; if consumers slow down, local buffers fill and risk OOM. In Kafka\'s pull model, consumers pull batches when ready, allowing graceful degradation and natural backpressure without broker intervention.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q46',
    category: 'database_platforms',
    company: 'Google',
    domain: 'Storage Engines & File Formats',
    topic: 'LSM-Tree Write Amplification & Leveled Compaction',
    question: 'In a Log-Structured Merge (LSM) storage engine (e.g. RocksDB, Cassandra), how does Leveled Compaction compare to Size-Tiered Compaction regarding Read Amplification and Space Amplification?',
    options: [
      'Leveled Compaction provides significantly lower space amplification (~10-20% overhead) and faster point lookups at the cost of higher write amplification',
      'Leveled Compaction requires 100% temporary disk space during major compactions',
      'Size-Tiered Compaction guarantees strictly O(1) read latency across all SSTables',
      'Both compaction strategies yield identical disk I/O metrics regardless of workload',
    ],
    correctIndex: 0,
    explanation: 'In Leveled Compaction, each level has non-overlapping key ranges (except L0). Point lookups touch at most one SSTable per level (low read amplification) and disk space overhead is bounded to ~10%. However, repeatedly rewriting data across levels increases Write Amplification (WA can be 10x-30x).',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q47',
    category: 'database_platforms',
    company: 'Amazon',
    domain: 'High-Availability Database Engines',
    topic: 'Amazon Aurora Distributed Storage Architecture',
    question: 'How does Amazon Aurora achieve high write throughput and sub-minute replica recovery while maintaining a 6-way replication across 3 Availability Zones (AZs)?',
    options: [
      'By offloading the database engine to "The Log is the Database": only redo log records are sent across the network to storage nodes, eliminating dirty page writes',
      'By utilizing distributed RAID-0 stripping over ephemeral EC2 instance stores',
      'By turning off crash recovery logging and relying on periodic S3 snapshots',
      'By delegating cross-AZ replication to client browser WebSockets',
    ],
    correctIndex: 0,
    explanation: 'Traditional relational databases write dirty pages, double-write buffers, and undo/redo logs over the network. Aurora\'s core breakthrough is "The Log is the Database": database compute nodes send ONLY small redo log records to custom storage nodes. Storage nodes apply logs asynchronously and independently.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q48',
    category: 'cloud_platform',
    company: 'Netflix',
    domain: 'Resilience Engineering',
    topic: 'Bulkhead Pattern Resource Isolation',
    question: 'How does the Bulkhead isolation pattern protect a mission-critical user authentication service when a downstream third-party recommendation API hangs indefinitely?',
    options: [
      'Assigns isolated thread pools / connection pools to each remote dependency so that exhausted threads from the failing recommendation API cannot starve the authentication service pools',
      'Automatically scales up Kubernetes pods until physical server memory is depleted',
      'Compresses all outbound HTTP requests using Brotli encoding',
      'Disables TLS encryption to reduce CPU context switching latency',
    ],
    correctIndex: 0,
    explanation: 'Named after ship watertight compartments (bulkheads), this pattern partitions thread pools and resource allocations per dependency. If the recommendation service stalls, only its dedicated pool (e.g. 10 threads) exhausts; the authentication thread pool remains fully responsive and unaffected.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q49',
    category: 'observability_aiops',
    company: 'Google',
    domain: 'Distributed Tracing & Telemetry',
    topic: 'W3C TraceContext & Head vs Tail Sampling',
    question: 'Why do modern distributed tracing systems (such as Jaeger and OpenTelemetry) adopt Tail-Based Sampling over Head-Based Sampling for high-scale observability?',
    options: [
      'Tail-based sampling collects trace segments in memory buffers and makes the sampling decision after the trace completes, guaranteeing capture of 100% of errors and latency outliers',
      'Head-based sampling consumes zero CPU cycles on service mesh proxies',
      'Tail-based sampling eliminates the requirement for Trace ID and Span ID headers',
      'Head-based sampling can predict downstream microservice latency before requests arrive',
    ],
    correctIndex: 0,
    explanation: 'Head-based sampling decides whether to sample at the ingress entry point before knowing if the request will fail or experience high latency (missing rare 500 errors). Tail-based sampling buffers spans in collectors and evaluates the trace after completion, retaining traces with HTTP 5xx codes or high p99 latency.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q50',
    category: 'database_platforms',
    company: 'Google',
    domain: 'Spanner & Global Consistency',
    topic: 'Google Spanner TrueTime API Commit Wait Rule',
    question: 'In Google Spanner, the TrueTime API returns a time interval [earliest, latest] where uncertainty epsilon is bounded by atomic clocks and GPS receivers. How does Spanner guarantee external consistency (linearizability) across global transactions?',
    options: [
      'Commit Wait Rule: the leader assigns commit timestamp s = TT.now().latest, and delays releasing locks until TT.now().earliest > s, ensuring s has passed in absolute real time everywhere',
      'By using NTP round-robin synchronization across public time servers',
      'By aborting all transactions that execute across more than one continent',
      'By enforcing single-threaded execution across a master database server in California',
    ],
    correctIndex: 0,
    explanation: 'Spanner guarantees strict serializability globally via the Commit Wait Rule: transaction T2 starting after T1 completes is guaranteed a timestamp s2 > s1. The leader picks timestamp s = TT.now().latest, and waits out the clock uncertainty until TT.now().earliest > s before committing, ensuring s has elapsed in true absolute time.',
    difficulty: 'Hard',
  },
];
