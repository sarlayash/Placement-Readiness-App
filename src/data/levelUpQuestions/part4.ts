import { LevelUpQuestion } from './part1';

export const LEVEL_UP_PART_4: LevelUpQuestion[] = [
  {
    id: 'lu_q76',
    category: 'network_engineering',
    company: 'Google',
    domain: 'Transport Protocols & Modern Web',
    topic: 'HTTP/3 & QUIC Head-of-Line Blocking Elimination',
    question: 'Why does HTTP/3 over QUIC (UDP) eliminate the TCP Head-of-Line (HoL) blocking problem that persists even in HTTP/2 over TLS?',
    options: [
      'In HTTP/2, all multiplexed streams share a single TCP connection; packet loss on one stream stalls the TCP window for all concurrent streams. QUIC implements independent packet loss recovery per stream at the transport layer',
      'HTTP/3 does not perform any packet retransmission or flow control',
      'QUIC compresses HTTP headers using gzip instead of HPACK',
      'UDP packets are legally immune to network congestion',
    ],
    correctIndex: 0,
    explanation: 'In HTTP/2, multiple logical requests are multiplexed over a single TCP byte stream. If a single TCP packet is dropped, TCP blocks delivery of all subsequent bytes until the missing packet is retransmitted (Transport HoL blocking). QUIC runs over UDP and handles reliability per-stream: a dropped packet on stream A delays only stream A; stream B continues without interruption.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q77',
    category: 'cybersecurity_iam',
    company: 'Google',
    domain: 'Transport Layer Cryptography',
    topic: 'TLS 1.3 0-RTT Early Data Replay Vulnerability',
    question: 'In TLS 1.3, what security vulnerability is introduced when clients utilize the 0-RTT (Zero Round Trip Time) Early Data resumption feature?',
    options: [
      '0-RTT data is vulnerable to Replay Attacks by network eavesdroppers because it lacks forward secrecy and can be replayed to the server to duplicate actions (e.g. duplicating payment or state mutations)',
      '0-RTT data is transmitted in unencrypted plaintext',
      'The server\'s private RSA key is exposed in the ServerHello message',
      'Clients cannot authenticate the server\'s X.509 certificate',
    ],
    correctIndex: 0,
    explanation: 'TLS 1.3 allows clients that previously connected to send application data in the very first flight (0-RTT) using cached pre-shared keys. Because this flight cannot have fresh Diffie-Hellman ephemeral keys on both sides, an attacker who intercepts the packet can replay it to the server. Therefore, 0-RTT must only be used for idempotent requests (safe GETs), never POST mutations.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q78',
    category: 'network_engineering',
    company: 'Google',
    domain: 'Internet Routing & DDoS Mitigation',
    topic: 'BGP Anycast Routing Architecture',
    question: 'How does BGP Anycast routing enable global DNS resolvers (e.g. 8.8.8.8, 1.1.1.1) and CDNs to mitigate massive volumetric DDoS attacks?',
    options: [
      'Multiple geographically distributed data centers advertise the identical IP address via BGP; internet routing tables route attack traffic to the topologically closest edge data center, naturally diffusing the attack volume across dozens of global points-of-presence (PoPs)',
      'BGP Anycast encrypts all incoming UDP packets with hardware quantum keys',
      'It forces all internet service providers (ISPs) to drop UDP packets exceeding 512 bytes',
      'It centralizes all global internet traffic into a single hardened mainframe in Switzerland',
    ],
    correctIndex: 0,
    explanation: 'With BGP Anycast, dozens or hundreds of edge points of presence (PoPs) announce the exact same IP prefix via BGP. Intermediate routers forward packets to the nearest PoP using standard shortest AS-path routing. During a DDoS attack, botnet traffic is localized to regional data centers, absorbing and scrubbing the traffic without overloading any single facility.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q79',
    category: 'network_engineering',
    company: 'Google',
    domain: 'TCP Congestion Control Algorithms',
    topic: 'Google BBR vs CUBIC & Bufferbloat',
    question: 'How does Google\'s BBR (Bottleneck Bandwidth and RTT) congestion control algorithm differ fundamentally from loss-based algorithms like TCP CUBIC and Reno?',
    options: [
      'BBR explicitly measures max bandwidth (BtlBw) and min propagation delay (RTprop) to keep queue occupancy near zero, preventing bufferbloat rather than treating packet loss as a primary congestion signal',
      'BBR sends packets at a fixed 10 Gbps rate regardless of network capacity',
      'CUBIC measures packet round-trip time while BBR ignores all time measurements',
      'BBR runs only on wireless 5G cellular modems',
    ],
    correctIndex: 0,
    explanation: 'Traditional algorithms (Reno, CUBIC) ramp up sending window until packets are dropped by full router buffers ("Bufferbloat"), causing high queueing latency. BBR creates an explicit model of the network by tracking the maximum delivery rate and minimum round-trip time, pacing packets to operate at the optimal Kleinrock operating point (zero buffer queue).',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q80',
    category: 'cybersecurity_iam',
    company: 'Stripe',
    domain: 'Browser Security & CORS Mechanics',
    topic: 'CORS Preflight Triggers & Credential Restrictions',
    question: 'When a web browser executes a cross-origin `fetch()` request with `credentials: "include"`, why does the browser reject the response if the server returns `Access-Control-Allow-Origin: *`?',
    options: [
      'The W3C CORS specification strictly prohibits wildcards (`*`) when credentials (cookies, HTTP auth headers) are included; the server must explicitly echo the requesting origin in `Access-Control-Allow-Origin` and set `Access-Control-Allow-Credentials: true`',
      'Wildcards in CORS headers crash the browser\'s JavaScript V8 parsing engine',
      'CORS only applies to server-to-server gRPC communications',
      'HTTP cookies cannot be transmitted across domains with different top-level domains',
    ],
    correctIndex: 0,
    explanation: 'For security reasons, allowing arbitrary websites to read authenticated responses via `*` with cookies would lead to catastrophic cross-site data theft. The CORS standard mandates that if `Access-Control-Allow-Credentials: true` is set, `Access-Control-Allow-Origin` CANNOT be `*`; it must be the exact origin (e.g. `https://app.domain.com`).',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q81',
    category: 'cybersecurity_iam',
    company: 'Google',
    domain: 'Modern Authentication & OAuth 2.0',
    topic: 'OAuth 2.0 PKCE (Proof Key for Code Exchange)',
    question: 'Why is PKCE (RFC 7636) mandatory for Single Page Apps (SPAs) and Mobile Apps authenticating via OAuth 2.0 Authorization Code Flow?',
    options: [
      'Public clients cannot securely store a client_secret; PKCE creates a cryptographically random code_verifier and code_challenge (SHA-256) ensuring an intercepted authorization code cannot be exchanged without the verifier',
      'PKCE eliminates the need for user passwords during login',
      'PKCE encrypts the entire browser LocalStorage database',
      'Authorization codes cannot be transmitted over HTTPS without PKCE headers',
    ],
    correctIndex: 0,
    explanation: 'In public clients (SPAs, mobile apps), client secrets cannot be kept confidential. If an attacker intercepts the authorization code (e.g. via custom URI scheme hijacking), they could trade it for tokens. PKCE forces the client to generate a secret `code_verifier`, send `code_challenge = SHA256(verifier)` in step 1, and present the raw `code_verifier` in step 2.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q82',
    category: 'cybersecurity_iam',
    company: 'Netflix',
    domain: 'Identity & JWT Cryptanalysis',
    topic: 'JWT Algorithm Confusion (RS256 vs HS256)',
    question: 'In the classic JWT algorithm confusion attack, how does an attacker forge arbitrary admin tokens when a server expecting an RS256 token accepts HS256?',
    options: [
      'The attacker changes the header to `{"alg": "HS256"}` and signs the forged payload using the server\'s publicly available RSA public key string as the HMAC secret key',
      'The attacker brute-forces the 2048-bit RSA private key using quantum computing',
      'The attacker modifies the expiration claim to a negative Unix epoch timestamp',
      'The attacker replaces the JWT payload with raw XML data',
    ],
    correctIndex: 0,
    explanation: 'If a vulnerable JWT library checks signatures using the algorithm specified in the token header without verifying against server configuration, an attacker can specify HS256 (symmetric). The server uses its configured key (the public RSA key) to verify the HMAC signature. Because the public key is publicly accessible, the attacker signs the forged token with it.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q83',
    category: 'cybersecurity_iam',
    company: 'Stripe',
    domain: 'Application Security & SQL Injection',
    topic: 'Second-Order SQL Injection Mechanics',
    question: 'How does a Second-Order SQL Injection attack bypass input sanitization performed at the initial insertion point?',
    options: [
      'Malicious payload is safely stored in the database (e.g. as a user name `admin\'--`); the vulnerability occurs later when the application retrieves the stored data and concatenates it unsafely into a secondary dynamic query',
      'The attacker executes two separate HTTP POST requests within one millisecond',
      'The SQL parser executes the query twice before checking authentication credentials',
      'The attacker injects SQL commands inside the TCP SYN packet options',
    ],
    correctIndex: 0,
    explanation: 'In second-order SQL injection, the initial input is stored cleanly (e.g. parameterized INSERT). Because it resides in the database, subsequent application code assumes it is trusted and concatenates it into another dynamic SQL query (e.g. password reset or audit report), triggering malicious execution.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q84',
    category: 'cybersecurity_iam',
    company: 'Google',
    domain: 'Browser Security & XSS Mitigation',
    topic: 'Strict Content Security Policy (CSP) Nonces',
    question: 'Why is a Nonce-based Strict Content Security Policy (e.g., `script-src \'nonce-rAnd0m\' \'strict-dynamic\'; object-src \'none\'`) considered the gold standard for defeating Cross-Site Scripting (XSS)?',
    options: [
      'Browsers execute only script tags bearing the cryptographically random, per-request unguessable nonce; \'strict-dynamic\' allows trusted scripts to load dependencies without requiring unsafe domain allowlists',
      'It forces browsers to compile JavaScript to WebAssembly before execution',
      'It completely disables all JavaScript execution in the browser',
      'Nonces are validated directly by public DNS certificate authorities',
    ],
    correctIndex: 0,
    explanation: 'Traditional domain allowlists (e.g. `script-src https://*.googleapis.com`) are frequently bypassed via JSONP endpoints or open redirects on whitelisted domains. Nonce-based CSP generates a fresh random cryptographic nonce per HTTP response. Inline injections by attackers lack the secret nonce and are blocked by the browser.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q85',
    category: 'network_engineering',
    company: 'Google',
    domain: 'DNS & Internet Cryptography',
    topic: 'DNSSEC Cryptographic Chain of Trust',
    question: 'In DNSSEC, how does a validating recursive resolver verify that a DNS answer for `example.com` has not been spoofed by a Man-in-the-Middle attacker?',
    options: [
      'Verifies the RRSIG signature of the record against the zone DNSKEY, and validates the DS (Delegation Signer) hash in the parent `.com` zone recursively up to the Root Zone trust anchor',
      'Sends a challenge-response ping to the root nameserver via ICMP',
      'Encrypts all DNS queries using asymmetric RSA-4096 certificates',
      'Requires every client computer to hold the private key of the top-level domain',
    ],
    correctIndex: 0,
    explanation: 'DNSSEC establishes a hierarchical chain of trust: RRsets are signed by RRSIG using the Zone-Signing Key (ZSK). The ZSK is signed by the Key-Signing Key (KSK). The hash of the KSK is published as a DS record in the parent zone (.com). This hierarchy continues to the ICANN Root Zone, whose public key is hardcoded in resolvers.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q86',
    category: 'network_engineering',
    company: 'Uber',
    domain: 'Real-Time Communication Protocols',
    topic: 'WebSockets vs Server-Sent Events (SSE) vs gRPC Streaming',
    question: 'For an engineering scenario requiring real-time updates from server to web browser (e.g. live ride tracking or stock tickers) with automatic reconnection, HTTP/2 multiplexing, and simple firewall traversal, why is Server-Sent Events (SSE) often architecturally superior to WebSockets?',
    options: [
      'SSE runs over standard HTTP/2, natively supports multiplexing without extra TCP ports, includes automatic browser reconnection with event IDs, and requires no custom binary framing protocol',
      'SSE provides full-duplex binary audio streaming with sub-microsecond latency',
      'WebSockets cannot function through corporate proxy servers or TLS connections',
      'SSE eliminates the need for web browsers to run JavaScript',
    ],
    correctIndex: 0,
    explanation: 'WebSockets require upgrading the connection to a raw TCP-like protocol, breaking HTTP/2 multiplexing (each WS takes a full TCP connection) and requiring manual heartbeat/reconnect implementations. SSE operates over standard HTTP, leveraging HTTP/2 stream multiplexing, native browser `EventSource` auto-reconnect, and built-in event IDs.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q87',
    category: 'coding',
    company: 'Netflix',
    domain: 'Modern Frontend Architecture',
    topic: 'Webpack Module Federation Singleton Runtime Resolution',
    question: 'In a micro-frontend architecture using Webpack Module Federation, how does the runtime resolve shared dependencies (e.g. `react` and `react-dom`) declared with `{ singleton: true, requiredVersion: "^18.2.0" }` across independent remote apps?',
    options: [
      'It initializes a single shared instance of the library in the global container scope; if version mismatch occurs, it emits a console warning and uses the highest compatible version rather than loading duplicate copies that break React Context',
      'It recompiles the entire web application inside an in-browser WebAssembly VM',
      'It executes each micro-frontend in an isolated iframe with separate DOM trees',
      'It replaces React hooks with raw DOM createElement API calls',
    ],
    correctIndex: 0,
    explanation: 'Libraries like React maintain module-level state (hooks dispatcher). If two micro-frontends load different physical copies of React, hooks fail catastrophically. Module Federation\'s `singleton: true` guarantees only one copy is instantiated in the shared scope, negotiating version semver constraints at runtime.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q88',
    category: 'network_engineering',
    company: 'Google',
    domain: 'Software Defined Networking',
    topic: 'Google Maglev Consistent Hashing Load Balancer',
    question: 'In Google\'s Maglev network load balancer, how does the lookup table algorithm ensure minimal packet disruption to existing TCP connections when load balancer nodes are added or removed from the cluster?',
    options: [
      'Each backend generates a deterministic pseudo-random permutation of the lookup table of prime size M; Maglev populates entries round-robin, ensuring consistent backend assignment without cross-node state synchronization',
      'Every packet is stored in an in-memory Redis cluster before forwarding to servers',
      'Maglev uses hardware IP-in-IP tunneling to broadcast all packets to all backends',
      'Backends hold active BGP peering sessions with client home routers',
    ],
    correctIndex: 0,
    explanation: 'Maglev routes packets at kernel line-rate without connection state synchronization. Each backend has a preference list (permutation of [0..M-1] where M is a large prime). Maglev fills an M-entry table by iterating preferences. Even when Maglev nodes fail or expand, the lookup table remains virtually identical across all nodes.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q89',
    category: 'cloud_platform',
    company: 'Amazon',
    domain: 'Serverless Runtimes & Virtualization',
    topic: 'V8 Isolates (Cloudflare) vs MicroVMs (AWS Firecracker)',
    question: 'How do V8 Isolates (used in Cloudflare Workers) achieve sub-millisecond cold starts compared to MicroVMs (used in AWS Lambda / Firecracker)?',
    options: [
      'V8 Isolates run thousands of tenant sandboxes within a single shared operating system process using V8 memory boundary enforcement, avoiding Linux kernel boot, virtual hardware initialization, and page table allocation',
      'V8 Isolates compile code directly to bare-metal hardware CPU microcode',
      'AWS Firecracker runs exclusively on macOS operating systems',
      'V8 Isolates disable garbage collection to achieve zero latency',
    ],
    correctIndex: 0,
    explanation: 'Firecracker creates a lightweight KVM-based microVM with its own minimal Linux kernel and virtual hardware (~5-10ms cold start). V8 Isolates run inside a single existing process: creating an Isolate is allocating a few hundred kilobytes of memory and setting up a V8 execution context (~0.5ms cold start), with process-level hardware isolation replaced by V8 security boundary checks.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q90',
    category: 'cybersecurity_iam',
    company: 'Google',
    domain: 'Enterprise Security Architecture',
    topic: 'Google BeyondCorp Zero-Trust Principles',
    question: 'What is the central architectural principle of Google\'s BeyondCorp Zero-Trust security model compared to traditional perimeter-based corporate VPNs?',
    options: [
      'Access to corporate applications depends on device state, user identity, and continuous context evaluation for EVERY request, regardless of whether the client is on the physical corporate network or public internet',
      'All employees must connect through an on-premises physical VPN gateway in California',
      'Corporate applications are accessible only using physical RSA hardware tokens',
      'Internal intranet traffic is transmitted without TLS encryption to improve speed',
    ],
    correctIndex: 0,
    explanation: 'Traditional security relies on perimeter defense ("castle and moat"): once inside the VPN, the user is trusted. BeyondCorp eliminates privileged networks: no device is trusted simply because of its network location. Access to every service is mediated by an access proxy evaluating user credentials, device health certificate, and context.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q91',
    category: 'ai_architecture',
    company: 'Google',
    domain: 'Generative AI & Inference Systems',
    topic: 'Transformer LLM Inference: Memory Bandwidth vs Compute Bound',
    question: 'In Large Language Model (LLM) inference (e.g. GPT-4, Gemini), why is the autoregressive token generation phase (decoding) strictly memory-bandwidth bound rather than compute bound?',
    options: [
      'Each generated token requires loading all billions of model weights from GPU High Bandwidth Memory (HBM) into SRAM for just a single batch/token multiplication, resulting in an arithmetic intensity well below the GPU\'s compute capability',
      'GPUs cannot perform floating point operations during text generation',
      'Token generation requires streaming training datasets from cloud object storage',
      'Attention matrices cannot be parallelized across CUDA cores',
    ],
    correctIndex: 0,
    explanation: 'In the prefill phase, all prompt tokens are processed simultaneously (GEMM matrix multiplication, high arithmetic intensity, compute bound). In decoding, each token is generated one-by-one: for a 70B parameter model in FP16, 140 GB of weights must be fetched from GPU HBM to compute ONE token for batch size 1, bottlenecked entirely by HBM bandwidth (TB/s).',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q92',
    category: 'ai_architecture',
    company: 'Meta',
    domain: 'Deep Learning Kernel Optimization',
    topic: 'FlashAttention Tiling & HBM I/O Complexity',
    question: 'How does FlashAttention (Dao et al.) achieve a 2x-4x speedup in Transformer self-attention without changing the exact mathematical output?',
    options: [
      'It tiles the Softmax and Attention computations into blocks fitting inside fast GPU SRAM (20 TB/s), avoiding materializing the full N x N attention matrix in slow GPU High Bandwidth Memory (HBM)',
      'It quantizes all weights to binary 1-bit integers',
      'It replaces the multi-head attention mechanism with linear convolutions',
      'It computes attention using CPU vector registers instead of CUDA cores',
    ],
    correctIndex: 0,
    explanation: 'Standard attention computes S = Q*K^T (writes N x N to HBM), P = softmax(S) (reads/writes N x N to HBM), and O = P*V. FlashAttention uses online softmax to compute attention in tiles within fast on-chip SRAM, reducing HBM memory reads/writes from O(N^2) to O(N * d), eliminating the memory bandwidth bottleneck.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q93',
    category: 'ai_architecture',
    company: 'Google',
    domain: 'AI Model Optimization & Compression',
    topic: 'Post-Training Quantization (AWQ vs GPTQ)',
    question: 'Why does Activation-aware Weight Quantization (AWQ) protect model perplexity in 4-bit integer quantization better than naive round-to-nearest quantization?',
    options: [
      'It identifies that the top 1% of salient weights correspond to channels with large activation magnitudes, scaling and protecting these critical weights while quantizing the remaining 99% of weights to INT4',
      'It rounds all model weights to the nearest power of two',
      'It retrains the foundational model on millions of tokens from scratch',
      'It discards all negative weight values from neural network layers',
    ],
    correctIndex: 0,
    explanation: 'Research demonstrated that not all weights in an LLM are equally important. Looking at activation channels reveals that a tiny fraction (0.1-1%) of weights handle massive activation outliers. AWQ protects these salient weights from precision loss, preserving model reasoning capabilities while shrinking memory footprint by 75%.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q94',
    category: 'ai_architecture',
    company: 'Google',
    domain: 'LLM Inference Optimization',
    topic: 'Speculative Decoding Acceleration Dynamics',
    question: 'In Speculative Decoding for LLM serving, how does a small Draft model accelerate the generation of a large Target model without altering the target model\'s output probability distribution?',
    options: [
      'The draft model rapidly speculates K tokens autoregressively; the large model evaluates all K tokens in a single parallel forward pass, accepting valid tokens using modified rejection sampling',
      'The draft model generates the entire response and the target model is skipped',
      'Both models run in lockstep alternating every odd and even word',
      'The draft model generates tokens only when the target model crashes',
    ],
    correctIndex: 0,
    explanation: 'Because evaluating K tokens in parallel is compute-bound and takes roughly the same time as generating 1 token autoregressively on the target model, a fast draft model speculates K tokens. The target model verifies them all in 1 forward pass. Rejection sampling ensures the output distribution matches the target model exactly.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q95',
    category: 'ai_architecture',
    company: 'Uber',
    domain: 'Vector Search & Embeddings',
    topic: 'HNSW (Hierarchical Navigable Small World) Complexity',
    question: 'Why has HNSW become the dominant algorithm for approximate nearest neighbor (ANN) vector search in vector databases (e.g. Pinecone, Milvus, Qdrant)?',
    options: [
      'It constructs a multi-layer graph with skip-list properties: top layers have long-range links for coarse routing, and bottom layers have high clustering for fine-grained O(log N) search with high recall',
      'It computes exact cosine similarity against all N database vectors in parallel',
      'It eliminates the need for vector embeddings and indexes raw string text',
      'It compresses vectors into 8-bit ASCII characters',
    ],
    correctIndex: 0,
    explanation: 'HNSW generalizes the 1D skip-list to geometric graphs. Higher layers contain sparse graphs with long-distance edges for rapid exploration across the vector space. Lower layers contain dense graphs with short edges for greedy local descent. This yields logarithmic O(log N) query time with >95% recall.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q96',
    category: 'ai_architecture',
    company: 'Microsoft',
    domain: 'Parameter-Efficient Fine-Tuning (PEFT)',
    topic: 'LoRA (Low-Rank Adaptation) Mathematical Formulation',
    question: 'In LoRA (Low-Rank Adaptation) fine-tuning of neural networks, for a pre-trained weight matrix W_0 in R^{d x k}, how is the weight update represented using low-rank decomposition?',
    options: [
      'Delta W = B * A, where B in R^{d x r}, A in R^{r x k}, and the intrinsic rank r << min(d, k), freezing W_0 and training only B and A',
      'Delta W = W_0^T * W_0 + I',
      'Delta W = diagonal(W_0) * learning_rate',
      'Delta W is computed using Singular Value Decomposition on every forward pass',
    ],
    correctIndex: 0,
    explanation: 'LoRA hypothesizes that weight changes during adaptation have a low "intrinsic dimension". Instead of retraining all d x k parameters, it freezes W_0 and trains two small matrices: A (initialized from Gaussian) and B (initialized to zero), with rank r (e.g. 8 or 16). This slashes trainable parameters and GPU optimizer VRAM by 99%.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q97',
    category: 'ai_architecture',
    company: 'OpenAI',
    domain: 'LLM Memory Management',
    topic: 'PagedAttention (vLLM) KV-Cache Virtualization',
    question: 'How does PagedAttention (as implemented in vLLM) solve the 60–80% GPU memory waste caused by conventional Key-Value (KV) cache allocation in LLM serving?',
    options: [
      'It partitions the KV-cache of each request into fixed-size virtual memory blocks (pages) mapped to physical GPU blocks, eliminating internal fragmentation and pre-allocated contiguous memory bounds',
      'It compresses KV-cache tensors onto CPU swap files using gzip',
      'It discards all attention keys for tokens preceding the last 5 words',
      'It converts floating-point KV matrices into boolean truth tables',
    ],
    correctIndex: 0,
    explanation: 'Traditional engines allocate large contiguous memory buffers for each request anticipating maximum sequence length (e.g. 2048 tokens). Because sequence lengths vary, 60-80% of VRAM is wasted in fragmentation. PagedAttention adapts OS virtual memory: KV tensors are stored in non-contiguous pages, allocated on-demand as tokens generate.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q98',
    category: 'ai_architecture',
    company: 'Meta',
    domain: 'Distributed Deep Learning Systems',
    topic: 'ZeRO (Zero Redundancy Optimizer) Memory Partitioning',
    question: 'In DeepSpeed / Megatron-LM distributed training, what does ZeRO Stage 3 partition across all GPU workers to eliminate memory redundancy while maintaining Data Parallelism?',
    options: [
      'Partitions Optimizer States (Stage 1), Gradients (Stage 2), AND Model Parameters (Stage 3), broadcasting parameter shards on-demand during forward and backward passes',
      'Partitions only the training dataset text files across disk folders',
      'Duplicates all model parameters across every GPU core',
      'Disables backward backpropagation passes completely',
    ],
    correctIndex: 0,
    explanation: 'Standard Data Parallelism duplicates optimizer states, gradients, and model weights across every GPU. ZeRO Stage 1 partitions optimizer states (4x memory reduction). ZeRO Stage 2 partitions gradients (8x reduction). ZeRO Stage 3 partitions all model parameters, gathering parameter shards via All-Gather only when needed during computation.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q99',
    category: 'network_engineering',
    company: 'Google',
    domain: 'High-Performance Cluster Networking',
    topic: 'RDMA over Converged Ethernet (RoCEv2) in AI Supercomputers',
    question: 'Why is RoCEv2 (RDMA over Converged Ethernet) with Priority Flow Control (PFC) essential for multi-node GPU cluster training (e.g. NVIDIA DGX SuperPODs)?',
    options: [
      'It allows GPUs on different servers to directly read/write each other\'s memory buffers over 400G/800G Ethernet with zero CPU involvement, zero OS kernel copies, and sub-microsecond latency',
      'It encrypts GPU memory with AES-256 before tensor cores execute',
      'It converts GPU tensor computations into standard HTTP/1.1 REST calls',
      'It allows GPUs to communicate over public consumer WiFi networks',
    ],
    correctIndex: 0,
    explanation: 'Training models across thousands of GPUs requires exchanging terabytes of gradients during All-Reduce operations. Traditional TCP introduces kernel context switches, buffer copies, and high latency. RDMA transfers data directly between GPU VRAM and network cards (GPUDirect RDMA) over lossless Ethernet (PFC), delivering line-rate speed.',
    difficulty: 'Hard',
  },
  {
    id: 'lu_q100',
    category: 'observability_aiops',
    company: 'Google',
    domain: 'Site Reliability Engineering (SRE)',
    topic: 'SRE Error Budget & Four Nines (99.99%) Downtime Bounds',
    question: 'Under a Service Level Objective (SLO) of 99.99% ("four nines") availability across a standard 30-day calendar month, what is the maximum total downtime allowed before the Error Budget is 100% depleted?',
    options: [
      'Exactly 4.32 minutes (or ~4 minutes 19 seconds) of total cumulative downtime',
      '43.2 minutes of downtime',
      '7.2 hours of downtime',
      'Zero seconds (no downtime is ever permitted)',
    ],
    correctIndex: 0,
    explanation: 'A 30-day month contains 30 * 24 * 60 = 43,200 minutes. With a 99.99% availability target, the allowed unreliability is 0.01% (0.0001). 43,200 * 0.0001 = 4.32 minutes (~259.2 seconds). Exceeding 4.32 minutes freezes feature deployments under standard Google SRE policy.',
    difficulty: 'Hard',
  },
];
